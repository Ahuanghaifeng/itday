<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.infomation.haiffeng">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>

    <application
        android:name=".base.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        
        <activity
            android:name=".App.MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:theme="@style/AppTheme.Base"
            android:name=".App.WebView.WebActivity"></activity>

        <activity
            android:theme="@style/AppTheme.Base"
            android:name=".App.gallery.ImgLookActivity"></activity>

        <activity
            android:theme="@style/AppTheme.Base"
            android:name=".App.about.AboutActivity"></activity>

        <activity
            android:theme="@style/AppTheme.Base"
            android:name=".App.about.DaShangActivity"></activity>
    </application>

</manifest>